With Python 3.10.6

--- SETUP ---

python3 -m venv venv
source venv/bin/activate

pip3 install -r requirements. txt

.env structure:

password=''
username=''

db_name=''
db_type=''

--- COMMANDS ---

uvicorn app.main:app --reload
NOTE: main as per the main file name. App as per the variable name inside the main file.

--- OTHERS ---

http://localhost:8000/openapi.json
http://localhost:8000/docs

if problems:
sudo apt install python3-dev libpq-dev